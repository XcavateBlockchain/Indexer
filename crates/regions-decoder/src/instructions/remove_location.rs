//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Deregister a postcode and release its recorded deposit. Region-operator-only.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct RemoveLocation {
    pub region_id: u16,
    pub postcode: Vec<u8>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RemoveLocationInstructionAccounts {
    pub operator: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub operator_role: solana_pubkey::Pubkey,
    pub xcav_mint: solana_pubkey::Pubkey,
    pub operator_token: solana_pubkey::Pubkey,
    pub vault: solana_pubkey::Pubkey,
    pub region: solana_pubkey::Pubkey,
    pub location: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl RemoveLocation {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[85, 2, 35, 12, 215, 19, 137, 25] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for RemoveLocation {
    type ArrangedAccounts = RemoveLocationInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let operator = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let operator_role = next_account(&mut iter)?;
        let xcav_mint = next_account(&mut iter)?;
        let operator_token = next_account(&mut iter)?;
        let vault = next_account(&mut iter)?;
        let region = next_account(&mut iter)?;
        let location = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(RemoveLocationInstructionAccounts {
            operator,
            config,
            operator_role,
            xcav_mint,
            operator_token,
            vault,
            region,
            location,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}
