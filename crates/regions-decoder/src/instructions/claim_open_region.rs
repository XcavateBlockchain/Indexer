//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Claim an open region seat, bonding 0.1% of XCAV supply plus the region's
/// location deposits, capped by the caller's `max_deposit`. First-come and
/// RegionalOperator-only; the incumbent may also call this to renew, paying
/// only the difference if the bond has moved since they last bonded.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct ClaimOpenRegion {
    pub region_id: u16,
    pub max_deposit: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ClaimOpenRegionInstructionAccounts {
    pub new_operator: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub operator_role: solana_pubkey::Pubkey,
    pub xcav_mint: solana_pubkey::Pubkey,
    pub new_operator_token: solana_pubkey::Pubkey,
    pub vault: solana_pubkey::Pubkey,
    pub region: solana_pubkey::Pubkey,
    pub old_owner: solana_pubkey::Pubkey,
    pub old_owner_token: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ClaimOpenRegion {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[207, 218, 1, 124, 8, 184, 146, 54] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ClaimOpenRegion {
    type ArrangedAccounts = ClaimOpenRegionInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let new_operator = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let operator_role = next_account(&mut iter)?;
        let xcav_mint = next_account(&mut iter)?;
        let new_operator_token = next_account(&mut iter)?;
        let vault = next_account(&mut iter)?;
        let region = next_account(&mut iter)?;
        let old_owner = next_account(&mut iter)?;
        let old_owner_token = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ClaimOpenRegionInstructionAccounts {
            new_operator,
            config,
            operator_role,
            xcav_mint,
            new_operator_token,
            vault,
            region,
            old_owner,
            old_owner_token,
            token_program,
            associated_token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
