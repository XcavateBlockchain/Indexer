//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::RegionsDecoder;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod accept_authority;
pub mod adjust_listing_duration;
pub mod adjust_region_tax;
pub mod claim_open_region;
pub mod clear_region_state;
pub mod create_new_location;
pub mod create_region;
pub mod finalize_region_proposal;
pub mod initialize_config;
pub mod initiate_resignation;
pub mod propose_new_region;
pub mod remove_location;
pub mod unlock_voting_token;
pub mod update_authority;
pub mod update_config;
pub mod vote_on_region_proposal;
pub mod cpi_event;

pub use self::accept_authority::*;
pub use self::adjust_listing_duration::*;
pub use self::adjust_region_tax::*;
pub use self::claim_open_region::*;
pub use self::clear_region_state::*;
pub use self::create_new_location::*;
pub use self::create_region::*;
pub use self::finalize_region_proposal::*;
pub use self::initialize_config::*;
pub use self::initiate_resignation::*;
pub use self::propose_new_region::*;
pub use self::remove_location::*;
pub use self::unlock_voting_token::*;
pub use self::update_authority::*;
pub use self::update_config::*;
pub use self::vote_on_region_proposal::*;
pub use self::cpi_event::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum RegionsInstruction {
    AcceptAuthority(AcceptAuthority),
    AdjustListingDuration(AdjustListingDuration),
    AdjustRegionTax(AdjustRegionTax),
    ClaimOpenRegion(ClaimOpenRegion),
    ClearRegionState(ClearRegionState),
    CreateNewLocation(CreateNewLocation),
    CreateRegion(CreateRegion),
    FinalizeRegionProposal(FinalizeRegionProposal),
    InitializeConfig(InitializeConfig),
    InitiateResignation(InitiateResignation),
    ProposeNewRegion(ProposeNewRegion),
    RemoveLocation(RemoveLocation),
    UnlockVotingToken(UnlockVotingToken),
    UpdateAuthority(UpdateAuthority),
    UpdateConfig(UpdateConfig),
    VoteOnRegionProposal(VoteOnRegionProposal),
    // Anchor CPI Event Instruction
    CpiEvent(CpiEvent)
}

impl carbon_core::instruction::InstructionDecoder<'_> for RegionsDecoder {
    type InstructionType = RegionsInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = accept_authority::AcceptAuthority::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::AcceptAuthority(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = adjust_listing_duration::AdjustListingDuration::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::AdjustListingDuration(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = adjust_region_tax::AdjustRegionTax::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::AdjustRegionTax(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = claim_open_region::ClaimOpenRegion::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::ClaimOpenRegion(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = clear_region_state::ClearRegionState::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::ClearRegionState(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_new_location::CreateNewLocation::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::CreateNewLocation(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = create_region::CreateRegion::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::CreateRegion(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = finalize_region_proposal::FinalizeRegionProposal::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::FinalizeRegionProposal(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = initialize_config::InitializeConfig::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::InitializeConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = initiate_resignation::InitiateResignation::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::InitiateResignation(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = propose_new_region::ProposeNewRegion::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::ProposeNewRegion(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_location::RemoveLocation::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::RemoveLocation(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = unlock_voting_token::UnlockVotingToken::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::UnlockVotingToken(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_authority::UpdateAuthority::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::UpdateAuthority(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_config::UpdateConfig::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::UpdateConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = vote_on_region_proposal::VoteOnRegionProposal::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::VoteOnRegionProposal(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = cpi_event::CpiEvent::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: RegionsInstruction::CpiEvent(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
