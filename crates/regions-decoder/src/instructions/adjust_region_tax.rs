//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Change the region's property sale tax. Region-operator-only.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct AdjustRegionTax {
    pub region_id: u16,
    pub tax_bps: u16,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AdjustRegionTaxInstructionAccounts {
    pub operator: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub operator_role: solana_pubkey::Pubkey,
    pub region: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl AdjustRegionTax {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[199, 105, 230, 191, 76, 22, 174, 100] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for AdjustRegionTax {
    type ArrangedAccounts = AdjustRegionTaxInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let operator = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let operator_role = next_account(&mut iter)?;
        let region = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(AdjustRegionTaxInstructionAccounts {
            operator,
            config,
            operator_role,
            region,
            remaining: remaining.to_vec(),
        })
    }
}
