//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

/// Claim a region whose proposal passed, creating it with its initial
/// listing duration and sale tax. Proposer-only.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CreateRegion {
    pub region_id: u16,
    pub listing_duration: i64,
    pub tax_bps: u16,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateRegionInstructionAccounts {
    pub creator: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub creator_role: solana_pubkey::Pubkey,
    pub region_state: solana_pubkey::Pubkey,
    pub region: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CreateRegion {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[108, 252, 183, 41, 46, 77, 84, 66] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for CreateRegion {
    type ArrangedAccounts = CreateRegionInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let creator = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let creator_role = next_account(&mut iter)?;
        let region_state = next_account(&mut iter)?;
        let region = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CreateRegionInstructionAccounts {
            creator,
            config,
            creator_role,
            region_state,
            region,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
