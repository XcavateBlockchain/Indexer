//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U16;
use carbon_core::postgres::primitives::U64;
use carbon_core::postgres::primitives::U8;
use crate::types::Vote;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct VoteRecordRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub proposal_id: U64,
    pub voter: Pubkey,
    pub region_id: U16,
    pub vote: sqlx::types::Json<Vote>,
    pub power: U64,
    pub expiry: i64,
    pub bump: U8,
}

impl VoteRecordRow {
    pub fn from_parts(source: crate::accounts::vote_record::VoteRecord, metadata: AccountMetadata) -> Self {
        Self {
            account_metadata: metadata.into(),
            proposal_id: source.proposal_id.into(),
            voter: source.voter.into(),
            region_id: source.region_id.into(),
            vote: sqlx::types::Json(source.vote.into()),
            power: source.power.into(),
            expiry: source.expiry.into(),
            bump: source.bump.into(),
        }
    }
}

impl TryFrom<VoteRecordRow> for crate::accounts::vote_record::VoteRecord {
    type Error = carbon_core::error::Error;
    fn try_from(source: VoteRecordRow) -> Result<Self, Self::Error> {
        Ok(Self {
            proposal_id: *source.proposal_id,
            voter: *source.voter,
            region_id: source.region_id.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert value from postgres primitive".to_string()))?,
            vote: source.vote.0,
            power: *source.power,
            expiry: source.expiry.into(),
            bump: source.bump.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert value from postgres primitive".to_string()))?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::vote_record::VoteRecord {
    fn table() -> &'static str {
        "vote_record_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "proposal_id",
            "voter",
            "region_id",
            "vote",
            "power",
            "expiry",
            "bump",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for VoteRecordRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO vote_record_account (
                "proposal_id",
                "voter",
                "region_id",
                "vote",
                "power",
                "expiry",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#)
        .bind(self.proposal_id.clone())
        .bind(self.voter.clone())
        .bind(self.region_id.clone())
        .bind(self.vote.clone())
        .bind(self.power.clone())
        .bind(self.expiry.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for VoteRecordRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO vote_record_account (
                "proposal_id",
                "voter",
                "region_id",
                "vote",
                "power",
                "expiry",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "proposal_id" = EXCLUDED."proposal_id",
                "voter" = EXCLUDED."voter",
                "region_id" = EXCLUDED."region_id",
                "vote" = EXCLUDED."vote",
                "power" = EXCLUDED."power",
                "expiry" = EXCLUDED."expiry",
                "bump" = EXCLUDED."bump",
                __slot = EXCLUDED.__slot
            "#)
        .bind(self.proposal_id.clone())
        .bind(self.voter.clone())
        .bind(self.region_id.clone())
        .bind(self.vote.clone())
        .bind(self.power.clone())
        .bind(self.expiry.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for VoteRecordRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM vote_record_account WHERE
                __pubkey = $1
            "#)
        .bind(key)
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for VoteRecordRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM vote_record_account WHERE
                __pubkey = $1
            "#)
        .bind(key)
        .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct VoteRecordMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for VoteRecordMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS vote_record_account (
                -- Account data
                "proposal_id" NUMERIC(20) NOT NULL,
                "voter" BYTEA NOT NULL,
                "region_id" INT4 NOT NULL,
                "vote" JSONB NOT NULL,
                "power" NUMERIC(20) NOT NULL,
                "expiry" INT8 NOT NULL,
                "bump" INT2 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS vote_record_account"#).execute(connection).await?;
        Ok(())
    }
}
