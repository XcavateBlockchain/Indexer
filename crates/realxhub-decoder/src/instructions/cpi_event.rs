//! This code was AUTOGENERATED using the Codama library.

use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
pub use super::super::events;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, carbon_core::borsh::BorshSerialize, carbon_core::borsh::BorshDeserialize, PartialEq)]
pub enum CpiEvent {
    HubCreated(events::hub_created::HubCreatedEvent),
    IncomeClaimed(events::income_claimed::IncomeClaimedEvent),
    SaleRecorded(events::sale_recorded::SaleRecordedEvent),
    SharesBought(events::shares_bought::SharesBoughtEvent),
    SharesDelisted(events::shares_delisted::SharesDelistedEvent),
    SharesListed(events::shares_listed::SharesListedEvent),
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CpiEventInstructionAccounts {
    pub program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CpiEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[228, 69, 165, 46, 81, 203, 154, 29] {
            return None;
        }

        let event_data = &data[8..];

                if let Some(decoded) = events::hub_created::HubCreatedEvent::decode(event_data) {
            return Some(CpiEvent::HubCreated(decoded));
        }
                if let Some(decoded) = events::income_claimed::IncomeClaimedEvent::decode(event_data) {
            return Some(CpiEvent::IncomeClaimed(decoded));
        }
                if let Some(decoded) = events::sale_recorded::SaleRecordedEvent::decode(event_data) {
            return Some(CpiEvent::SaleRecorded(decoded));
        }
                if let Some(decoded) = events::shares_bought::SharesBoughtEvent::decode(event_data) {
            return Some(CpiEvent::SharesBought(decoded));
        }
                if let Some(decoded) = events::shares_delisted::SharesDelistedEvent::decode(event_data) {
            return Some(CpiEvent::SharesDelisted(decoded));
        }
                if let Some(decoded) = events::shares_listed::SharesListedEvent::decode(event_data) {
            return Some(CpiEvent::SharesListed(decoded));
        }
                None
    }
}

impl ArrangeAccounts for CpiEvent {
    type ArrangedAccounts = CpiEventInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let [program, event_authority, remaining @ ..] = accounts else {
            return None;
        };

        Some(CpiEventInstructionAccounts {
            program: program.pubkey,
            event_authority: event_authority.pubkey,
            remaining: remaining.to_vec(),
        })
    }
}

