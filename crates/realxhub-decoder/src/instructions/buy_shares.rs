//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct BuyShares {
    pub hub_id: u64,
    pub amount: u32,
    pub max_cost: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct BuySharesInstructionAccounts {
    pub buyer: solana_pubkey::Pubkey,
    pub seller: solana_pubkey::Pubkey,
    pub hub: solana_pubkey::Pubkey,
    pub listing: solana_pubkey::Pubkey,
    pub seller_holding: solana_pubkey::Pubkey,
    pub buyer_holding: solana_pubkey::Pubkey,
    pub share_mint: solana_pubkey::Pubkey,
    pub seller_share_account: solana_pubkey::Pubkey,
    pub buyer_share_account: solana_pubkey::Pubkey,
    pub stable_mint: solana_pubkey::Pubkey,
    pub buyer_stable: solana_pubkey::Pubkey,
    pub seller_stable: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl BuyShares {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[40, 239, 138, 154, 8, 37, 106, 108] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for BuyShares {
    type ArrangedAccounts = BuySharesInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let buyer = next_account(&mut iter)?;
        let seller = next_account(&mut iter)?;
        let hub = next_account(&mut iter)?;
        let listing = next_account(&mut iter)?;
        let seller_holding = next_account(&mut iter)?;
        let buyer_holding = next_account(&mut iter)?;
        let share_mint = next_account(&mut iter)?;
        let seller_share_account = next_account(&mut iter)?;
        let buyer_share_account = next_account(&mut iter)?;
        let stable_mint = next_account(&mut iter)?;
        let buyer_stable = next_account(&mut iter)?;
        let seller_stable = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(BuySharesInstructionAccounts {
            buyer,
            seller,
            hub,
            listing,
            seller_holding,
            buyer_holding,
            share_mint,
            seller_share_account,
            buyer_share_account,
            stable_mint,
            buyer_stable,
            seller_stable,
            token_program,
            associated_token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
