//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct RecordSale {
    pub hub_id: u64,
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RecordSaleInstructionAccounts {
    pub payer: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub hub: solana_pubkey::Pubkey,
    pub stable_mint: solana_pubkey::Pubkey,
    pub payer_stable: solana_pubkey::Pubkey,
    pub supplier: solana_pubkey::Pubkey,
    pub supplier_stable: solana_pubkey::Pubkey,
    pub operators: solana_pubkey::Pubkey,
    pub operators_stable: solana_pubkey::Pubkey,
    pub protocol: solana_pubkey::Pubkey,
    pub protocol_stable: solana_pubkey::Pubkey,
    pub income_vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl RecordSale {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[224, 117, 233, 68, 233, 154, 0, 29] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for RecordSale {
    type ArrangedAccounts = RecordSaleInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let payer = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let hub = next_account(&mut iter)?;
        let stable_mint = next_account(&mut iter)?;
        let payer_stable = next_account(&mut iter)?;
        let supplier = next_account(&mut iter)?;
        let supplier_stable = next_account(&mut iter)?;
        let operators = next_account(&mut iter)?;
        let operators_stable = next_account(&mut iter)?;
        let protocol = next_account(&mut iter)?;
        let protocol_stable = next_account(&mut iter)?;
        let income_vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(RecordSaleInstructionAccounts {
            payer,
            config,
            hub,
            stable_mint,
            payer_stable,
            supplier,
            supplier_stable,
            operators,
            operators_stable,
            protocol,
            protocol_stable,
            income_vault,
            token_program,
            associated_token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
