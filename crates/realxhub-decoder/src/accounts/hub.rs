//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct Hub {
    pub id: u64,
    pub name: String,
    pub share_mint: Pubkey,
    /// The operational SPV: first holder of all 100 shares.
    pub operational_spv: Pubkey,
    pub supplier: Pubkey,
    pub operators: Pubkey,
    pub protocol: Pubkey,
    pub per_wallet_cap: u32,
    /// Cumulative stablecoin income per share since the hub was created.
    pub income_per_share: u128,
    /// Remainder of holder legs that didn't divide by the supply; carried
    /// into the next sale so nothing is lost.
    pub income_dust: u64,
    pub bump: u8,
}

impl Hub {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[143, 189, 4, 120, 169, 234, 14, 125] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}
