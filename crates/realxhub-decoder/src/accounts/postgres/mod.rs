//! This code was AUTOGENERATED using the Codama library.
pub mod config_row;
pub mod faucet_receipt_row;
pub mod holding_row;
pub mod hub_row;
pub mod share_listing_row;

pub use self::config_row::*;
pub use self::faucet_receipt_row::*;
pub use self::holding_row::*;
pub use self::hub_row::*;
pub use self::share_listing_row::*;

use super::RealxhubAccount;

pub struct RealxhubAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for RealxhubAccountsMigration {
    fn app(&self) -> &str {
        "realxhub"
    }

    fn name(&self) -> &str {
        "realxhub_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(ConfigMigrationOperation),
            Box::new(FaucetReceiptMigrationOperation),
            Box::new(HoldingMigrationOperation),
            Box::new(HubMigrationOperation),
            Box::new(ShareListingMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct RealxhubAccountWithMetadata(pub RealxhubAccount, pub carbon_core::account::AccountMetadata);

impl From<(RealxhubAccount, carbon_core::account::AccountMetadata)> for RealxhubAccountWithMetadata {
    fn from(value: (RealxhubAccount, carbon_core::account::AccountMetadata)) -> Self {
        RealxhubAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RealxhubAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RealxhubAccountWithMetadata(account, metadata) = self;

        match account {
            RealxhubAccount::Config(account) => {
                let row = config_row::ConfigRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RealxhubAccount::FaucetReceipt(account) => {
                let row = faucet_receipt_row::FaucetReceiptRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RealxhubAccount::Holding(account) => {
                let row = holding_row::HoldingRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RealxhubAccount::Hub(account) => {
                let row = hub_row::HubRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RealxhubAccount::ShareListing(account) => {
                let row = share_listing_row::ShareListingRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RealxhubAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RealxhubAccountWithMetadata(account, metadata) = self;
        match account {
            RealxhubAccount::Config(account) => {
                let row = config_row::ConfigRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RealxhubAccount::FaucetReceipt(account) => {
                let row = faucet_receipt_row::FaucetReceiptRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RealxhubAccount::Holding(account) => {
                let row = holding_row::HoldingRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RealxhubAccount::Hub(account) => {
                let row = hub_row::HubRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RealxhubAccount::ShareListing(account) => {
                let row = share_listing_row::ShareListingRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

