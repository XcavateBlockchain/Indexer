//! This code was AUTOGENERATED using the Codama library.
use crate::MarketplaceDecoder;
use crate::PROGRAM_ID;

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod config;
pub mod investor_position;
pub mod lawyer;
pub mod lawyer_candidacy;
pub mod lawyer_vote;
pub mod listing;
pub mod property_asset;
pub mod reservation;
pub mod share_holding;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum MarketplaceAccount {
    Config(Box<config::Config>),
    InvestorPosition(Box<investor_position::InvestorPosition>),
    Lawyer(Box<lawyer::Lawyer>),
    LawyerCandidacy(Box<lawyer_candidacy::LawyerCandidacy>),
    LawyerVote(Box<lawyer_vote::LawyerVote>),
    Listing(Box<listing::Listing>),
    PropertyAsset(Box<property_asset::PropertyAsset>),
    Reservation(Box<reservation::Reservation>),
    ShareHolding(Box<share_holding::ShareHolding>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for MarketplaceDecoder {
    type AccountType = MarketplaceAccount;
    
    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }
        
        let data = account.data.as_slice();
        
        {
            if let Some(decoded) = config::Config::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::Config(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = investor_position::InvestorPosition::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::InvestorPosition(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = lawyer::Lawyer::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::Lawyer(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = lawyer_candidacy::LawyerCandidacy::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::LawyerCandidacy(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = lawyer_vote::LawyerVote::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::LawyerVote(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = listing::Listing::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::Listing(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = property_asset::PropertyAsset::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::PropertyAsset(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = reservation::Reservation::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::Reservation(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = share_holding::ShareHolding::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: MarketplaceAccount::ShareHolding(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        
        None
    }
}

