//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U32;
use carbon_core::postgres::primitives::U64;
use carbon_core::postgres::primitives::U8;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct LawyerCandidacyRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub listing_id: U64,
    pub round: U64,
    pub lawyer: Pubkey,
    pub costs: U64,
    pub vote_power: U32,
    pub rent_payer: Pubkey,
    pub bump: U8,
}

impl LawyerCandidacyRow {
    pub fn from_parts(source: crate::accounts::lawyer_candidacy::LawyerCandidacy, metadata: AccountMetadata) -> Self {
        Self {
            account_metadata: metadata.into(),
            listing_id: source.listing_id.into(),
            round: source.round.into(),
            lawyer: source.lawyer.into(),
            costs: source.costs.into(),
            vote_power: source.vote_power.into(),
            rent_payer: source.rent_payer.into(),
            bump: source.bump.into(),
        }
    }
}

impl TryFrom<LawyerCandidacyRow> for crate::accounts::lawyer_candidacy::LawyerCandidacy {
    type Error = carbon_core::error::Error;
    fn try_from(source: LawyerCandidacyRow) -> Result<Self, Self::Error> {
        Ok(Self {
            listing_id: *source.listing_id,
            round: *source.round,
            lawyer: *source.lawyer,
            costs: *source.costs,
            vote_power: source.vote_power.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert value from postgres primitive".to_string()))?,
            rent_payer: *source.rent_payer,
            bump: source.bump.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert value from postgres primitive".to_string()))?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::lawyer_candidacy::LawyerCandidacy {
    fn table() -> &'static str {
        "lawyer_candidacy_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "listing_id",
            "round",
            "lawyer",
            "costs",
            "vote_power",
            "rent_payer",
            "bump",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for LawyerCandidacyRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO lawyer_candidacy_account (
                "listing_id",
                "round",
                "lawyer",
                "costs",
                "vote_power",
                "rent_payer",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#)
        .bind(self.listing_id.clone())
        .bind(self.round.clone())
        .bind(self.lawyer.clone())
        .bind(self.costs.clone())
        .bind(self.vote_power.clone())
        .bind(self.rent_payer.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for LawyerCandidacyRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO lawyer_candidacy_account (
                "listing_id",
                "round",
                "lawyer",
                "costs",
                "vote_power",
                "rent_payer",
                "bump",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "listing_id" = EXCLUDED."listing_id",
                "round" = EXCLUDED."round",
                "lawyer" = EXCLUDED."lawyer",
                "costs" = EXCLUDED."costs",
                "vote_power" = EXCLUDED."vote_power",
                "rent_payer" = EXCLUDED."rent_payer",
                "bump" = EXCLUDED."bump",
                __slot = EXCLUDED.__slot
            "#)
        .bind(self.listing_id.clone())
        .bind(self.round.clone())
        .bind(self.lawyer.clone())
        .bind(self.costs.clone())
        .bind(self.vote_power.clone())
        .bind(self.rent_payer.clone())
        .bind(self.bump.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for LawyerCandidacyRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM lawyer_candidacy_account WHERE
                __pubkey = $1
            "#)
        .bind(key)
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for LawyerCandidacyRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM lawyer_candidacy_account WHERE
                __pubkey = $1
            "#)
        .bind(key)
        .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct LawyerCandidacyMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for LawyerCandidacyMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS lawyer_candidacy_account (
                -- Account data
                "listing_id" NUMERIC(20) NOT NULL,
                "round" NUMERIC(20) NOT NULL,
                "lawyer" BYTEA NOT NULL,
                "costs" NUMERIC(20) NOT NULL,
                "vote_power" INT8 NOT NULL,
                "rent_payer" BYTEA NOT NULL,
                "bump" INT2 NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS lawyer_candidacy_account"#).execute(connection).await?;
        Ok(())
    }
}
