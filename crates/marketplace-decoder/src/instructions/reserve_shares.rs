//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct ReserveShares {
    pub listing_id: u64,
    pub amount: u32,
    pub max_total_cost: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ReserveSharesInstructionAccounts {
    pub investor: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub investor_role: solana_pubkey::Pubkey,
    pub listing: solana_pubkey::Pubkey,
    pub property: solana_pubkey::Pubkey,
    pub position: solana_pubkey::Pubkey,
    pub payment_mint: solana_pubkey::Pubkey,
    pub investor_payment: solana_pubkey::Pubkey,
    pub reservation: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ReserveShares {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[137, 47, 218, 50, 106, 149, 133, 110] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ReserveShares {
    type ArrangedAccounts = ReserveSharesInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let investor = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let investor_role = next_account(&mut iter)?;
        let listing = next_account(&mut iter)?;
        let property = next_account(&mut iter)?;
        let position = next_account(&mut iter)?;
        let payment_mint = next_account(&mut iter)?;
        let investor_payment = next_account(&mut iter)?;
        let reservation = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ReserveSharesInstructionAccounts {
            investor,
            payer,
            config,
            investor_role,
            listing,
            property,
            position,
            payment_mint,
            investor_payment,
            reservation,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
