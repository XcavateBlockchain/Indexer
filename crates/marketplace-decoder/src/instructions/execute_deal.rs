//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct ExecuteDeal {
    pub listing_id: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ExecuteDealInstructionAccounts {
    pub cranker: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub listing: solana_pubkey::Pubkey,
    pub property: solana_pubkey::Pubkey,
    pub region: solana_pubkey::Pubkey,
    pub developer_lawyer_registry: solana_pubkey::Pubkey,
    pub spv_lawyer_registry: solana_pubkey::Pubkey,
    pub listing_vault: solana_pubkey::Pubkey,
    pub xcav_mint: solana_pubkey::Pubkey,
    pub developer_token: solana_pubkey::Pubkey,
    pub vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub payment_token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ExecuteDeal {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[107, 92, 176, 235, 53, 80, 14, 94] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ExecuteDeal {
    type ArrangedAccounts = ExecuteDealInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let cranker = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let listing = next_account(&mut iter)?;
        let property = next_account(&mut iter)?;
        let region = next_account(&mut iter)?;
        let developer_lawyer_registry = next_account(&mut iter)?;
        let spv_lawyer_registry = next_account(&mut iter)?;
        let listing_vault = next_account(&mut iter)?;
        let xcav_mint = next_account(&mut iter)?;
        let developer_token = next_account(&mut iter)?;
        let vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let payment_token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ExecuteDealInstructionAccounts {
            cranker,
            config,
            listing,
            property,
            region,
            developer_lawyer_registry,
            spv_lawyer_registry,
            listing_vault,
            xcav_mint,
            developer_token,
            vault,
            token_program,
            payment_token_program,
            remaining: remaining.to_vec(),
        })
    }
}
