//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct VoteOnSpvLawyer {
    pub listing_id: u64,
    pub amount: u32,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct VoteOnSpvLawyerInstructionAccounts {
    pub voter: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub voter_role: solana_pubkey::Pubkey,
    pub listing: solana_pubkey::Pubkey,
    pub holding: solana_pubkey::Pubkey,
    pub vote_record: solana_pubkey::Pubkey,
    pub candidacy: solana_pubkey::Pubkey,
    pub previous_candidacy: Option<solana_pubkey::Pubkey>,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl VoteOnSpvLawyer {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[245, 181, 143, 227, 83, 159, 141, 147] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for VoteOnSpvLawyer {
    type ArrangedAccounts = VoteOnSpvLawyerInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let voter = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let voter_role = next_account(&mut iter)?;
        let listing = next_account(&mut iter)?;
        let holding = next_account(&mut iter)?;
        let vote_record = next_account(&mut iter)?;
        let candidacy = next_account(&mut iter)?;
        let previous_candidacy = next_account(&mut iter);
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(VoteOnSpvLawyerInstructionAccounts {
            voter,
            payer,
            voter_role,
            listing,
            holding,
            vote_record,
            candidacy,
            previous_candidacy,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
