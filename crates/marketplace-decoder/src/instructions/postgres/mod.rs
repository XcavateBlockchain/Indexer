//! This code was AUTOGENERATED using the Codama library.
pub mod accept_authority_row;
pub mod accept_offer_row;
pub mod assign_developer_lawyer_row;
pub mod buy_property_shares_row;
pub mod buy_relisted_shares_row;
pub mod cancel_offer_row;
pub mod claim_shares_row;
pub mod claim_spv_case_row;
pub mod close_cancelled_position_row;
pub mod close_candidacy_row;
pub mod close_case_row;
pub mod close_dead_listing_row;
pub mod close_reservation_row;
pub mod close_share_holding_row;
pub mod create_spv_row;
pub mod delist_shares_row;
pub mod execute_deal_row;
pub mod finalize_spv_election_row;
pub mod initialize_config_row;
pub mod init_property_assets_row;
pub mod lawyer_confirm_documents_row;
pub mod list_property_row;
pub mod lock_shares_row;
pub mod make_offer_row;
pub mod register_lawyer_row;
pub mod reject_offer_row;
pub mod release_reservation_row;
pub mod relist_shares_row;
pub mod reserve_shares_row;
pub mod resign_from_case_row;
pub mod resolve_silent_verdict_row;
pub mod send_property_shares_row;
pub mod settle_cancelled_fees_row;
pub mod unlock_shares_row;
pub mod unlock_voting_shares_row;
pub mod unregister_lawyer_row;
pub mod unreserve_shares_row;
pub mod update_authority_row;
pub mod update_config_row;
pub mod upgrade_object_row;
pub mod vote_on_spv_lawyer_row;
pub mod withdraw_cancelled_row;
pub mod withdraw_deposit_unsold_row;
pub mod withdraw_expired_row;
pub mod withdraw_legal_process_expired_row;
pub mod cpi_event_row;

pub use self::accept_authority_row::*;
pub use self::accept_offer_row::*;
pub use self::assign_developer_lawyer_row::*;
pub use self::buy_property_shares_row::*;
pub use self::buy_relisted_shares_row::*;
pub use self::cancel_offer_row::*;
pub use self::claim_shares_row::*;
pub use self::claim_spv_case_row::*;
pub use self::close_cancelled_position_row::*;
pub use self::close_candidacy_row::*;
pub use self::close_case_row::*;
pub use self::close_dead_listing_row::*;
pub use self::close_reservation_row::*;
pub use self::close_share_holding_row::*;
pub use self::create_spv_row::*;
pub use self::delist_shares_row::*;
pub use self::execute_deal_row::*;
pub use self::finalize_spv_election_row::*;
pub use self::initialize_config_row::*;
pub use self::init_property_assets_row::*;
pub use self::lawyer_confirm_documents_row::*;
pub use self::list_property_row::*;
pub use self::lock_shares_row::*;
pub use self::make_offer_row::*;
pub use self::register_lawyer_row::*;
pub use self::reject_offer_row::*;
pub use self::release_reservation_row::*;
pub use self::relist_shares_row::*;
pub use self::reserve_shares_row::*;
pub use self::resign_from_case_row::*;
pub use self::resolve_silent_verdict_row::*;
pub use self::send_property_shares_row::*;
pub use self::settle_cancelled_fees_row::*;
pub use self::unlock_shares_row::*;
pub use self::unlock_voting_shares_row::*;
pub use self::unregister_lawyer_row::*;
pub use self::unreserve_shares_row::*;
pub use self::update_authority_row::*;
pub use self::update_config_row::*;
pub use self::upgrade_object_row::*;
pub use self::vote_on_spv_lawyer_row::*;
pub use self::withdraw_cancelled_row::*;
pub use self::withdraw_deposit_unsold_row::*;
pub use self::withdraw_expired_row::*;
pub use self::withdraw_legal_process_expired_row::*;
pub use self::cpi_event_row::*;

use super::MarketplaceInstruction;

pub struct MarketplaceInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for MarketplaceInstructionsMigration {
    fn app(&self) -> &str {
        "marketplace"
    }

    fn name(&self) -> &str {
        "marketplace_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AcceptAuthorityMigrationOperation),
            Box::new(AcceptOfferMigrationOperation),
            Box::new(AssignDeveloperLawyerMigrationOperation),
            Box::new(BuyPropertySharesMigrationOperation),
            Box::new(BuyRelistedSharesMigrationOperation),
            Box::new(CancelOfferMigrationOperation),
            Box::new(ClaimSharesMigrationOperation),
            Box::new(ClaimSpvCaseMigrationOperation),
            Box::new(CloseCancelledPositionMigrationOperation),
            Box::new(CloseCandidacyMigrationOperation),
            Box::new(CloseCaseMigrationOperation),
            Box::new(CloseDeadListingMigrationOperation),
            Box::new(CloseReservationMigrationOperation),
            Box::new(CloseShareHoldingMigrationOperation),
            Box::new(CreateSpvMigrationOperation),
            Box::new(DelistSharesMigrationOperation),
            Box::new(ExecuteDealMigrationOperation),
            Box::new(FinalizeSpvElectionMigrationOperation),
            Box::new(InitializeConfigMigrationOperation),
            Box::new(InitPropertyAssetsMigrationOperation),
            Box::new(LawyerConfirmDocumentsMigrationOperation),
            Box::new(ListPropertyMigrationOperation),
            Box::new(LockSharesMigrationOperation),
            Box::new(MakeOfferMigrationOperation),
            Box::new(RegisterLawyerMigrationOperation),
            Box::new(RejectOfferMigrationOperation),
            Box::new(ReleaseReservationMigrationOperation),
            Box::new(RelistSharesMigrationOperation),
            Box::new(ReserveSharesMigrationOperation),
            Box::new(ResignFromCaseMigrationOperation),
            Box::new(ResolveSilentVerdictMigrationOperation),
            Box::new(SendPropertySharesMigrationOperation),
            Box::new(SettleCancelledFeesMigrationOperation),
            Box::new(UnlockSharesMigrationOperation),
            Box::new(UnlockVotingSharesMigrationOperation),
            Box::new(UnregisterLawyerMigrationOperation),
            Box::new(UnreserveSharesMigrationOperation),
            Box::new(UpdateAuthorityMigrationOperation),
            Box::new(UpdateConfigMigrationOperation),
            Box::new(UpgradeObjectMigrationOperation),
            Box::new(VoteOnSpvLawyerMigrationOperation),
            Box::new(WithdrawCancelledMigrationOperation),
            Box::new(WithdrawDepositUnsoldMigrationOperation),
            Box::new(WithdrawExpiredMigrationOperation),
            Box::new(WithdrawLegalProcessExpiredMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct MarketplaceInstructionWithMetadata(pub MarketplaceInstruction, pub carbon_core::instruction::InstructionMetadata);

impl From<(MarketplaceInstruction, carbon_core::instruction::InstructionMetadata)> for MarketplaceInstructionWithMetadata {
    fn from(value: (MarketplaceInstruction, carbon_core::instruction::InstructionMetadata)) -> Self {
        MarketplaceInstructionWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for MarketplaceInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MarketplaceInstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            MarketplaceInstruction::AcceptAuthority(instruction) => {
                let row = accept_authority_row::AcceptAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::AcceptOffer(instruction) => {
                let row = accept_offer_row::AcceptOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::AssignDeveloperLawyer(instruction) => {
                let row = assign_developer_lawyer_row::AssignDeveloperLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::BuyPropertyShares(instruction) => {
                let row = buy_property_shares_row::BuyPropertySharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::BuyRelistedShares(instruction) => {
                let row = buy_relisted_shares_row::BuyRelistedSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CancelOffer(instruction) => {
                let row = cancel_offer_row::CancelOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ClaimShares(instruction) => {
                let row = claim_shares_row::ClaimSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ClaimSpvCase(instruction) => {
                let row = claim_spv_case_row::ClaimSpvCaseRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseCancelledPosition(instruction) => {
                let row = close_cancelled_position_row::CloseCancelledPositionRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseCandidacy(instruction) => {
                let row = close_candidacy_row::CloseCandidacyRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseCase(instruction) => {
                let row = close_case_row::CloseCaseRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseDeadListing(instruction) => {
                let row = close_dead_listing_row::CloseDeadListingRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseReservation(instruction) => {
                let row = close_reservation_row::CloseReservationRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseShareHolding(instruction) => {
                let row = close_share_holding_row::CloseShareHoldingRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CreateSpv(instruction) => {
                let row = create_spv_row::CreateSpvRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::DelistShares(instruction) => {
                let row = delist_shares_row::DelistSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ExecuteDeal(instruction) => {
                let row = execute_deal_row::ExecuteDealRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::FinalizeSpvElection(instruction) => {
                let row = finalize_spv_election_row::FinalizeSpvElectionRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::InitPropertyAssets(instruction) => {
                let row = init_property_assets_row::InitPropertyAssetsRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::InitializeConfig(instruction) => {
                let row = initialize_config_row::InitializeConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::LawyerConfirmDocuments(instruction) => {
                let row = lawyer_confirm_documents_row::LawyerConfirmDocumentsRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ListProperty(instruction) => {
                let row = list_property_row::ListPropertyRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::LockShares(instruction) => {
                let row = lock_shares_row::LockSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::MakeOffer(instruction) => {
                let row = make_offer_row::MakeOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::RegisterLawyer(instruction) => {
                let row = register_lawyer_row::RegisterLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::RejectOffer(instruction) => {
                let row = reject_offer_row::RejectOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ReleaseReservation(instruction) => {
                let row = release_reservation_row::ReleaseReservationRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::RelistShares(instruction) => {
                let row = relist_shares_row::RelistSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ReserveShares(instruction) => {
                let row = reserve_shares_row::ReserveSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ResignFromCase(instruction) => {
                let row = resign_from_case_row::ResignFromCaseRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ResolveSilentVerdict(instruction) => {
                let row = resolve_silent_verdict_row::ResolveSilentVerdictRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::SendPropertyShares(instruction) => {
                let row = send_property_shares_row::SendPropertySharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::SettleCancelledFees(instruction) => {
                let row = settle_cancelled_fees_row::SettleCancelledFeesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnlockShares(instruction) => {
                let row = unlock_shares_row::UnlockSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnlockVotingShares(instruction) => {
                let row = unlock_voting_shares_row::UnlockVotingSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnregisterLawyer(instruction) => {
                let row = unregister_lawyer_row::UnregisterLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnreserveShares(instruction) => {
                let row = unreserve_shares_row::UnreserveSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UpdateAuthority(instruction) => {
                let row = update_authority_row::UpdateAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UpdateConfig(instruction) => {
                let row = update_config_row::UpdateConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UpgradeObject(instruction) => {
                let row = upgrade_object_row::UpgradeObjectRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::VoteOnSpvLawyer(instruction) => {
                let row = vote_on_spv_lawyer_row::VoteOnSpvLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawCancelled(instruction) => {
                let row = withdraw_cancelled_row::WithdrawCancelledRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawDepositUnsold(instruction) => {
                let row = withdraw_deposit_unsold_row::WithdrawDepositUnsoldRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawExpired(instruction) => {
                let row = withdraw_expired_row::WithdrawExpiredRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawLegalProcessExpired(instruction) => {
                let row = withdraw_legal_process_expired_row::WithdrawLegalProcessExpiredRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CpiEvent(instruction) => {
                let row = cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for MarketplaceInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MarketplaceInstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            MarketplaceInstruction::AcceptAuthority(instruction) => {
                let row = accept_authority_row::AcceptAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::AcceptOffer(instruction) => {
                let row = accept_offer_row::AcceptOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::AssignDeveloperLawyer(instruction) => {
                let row = assign_developer_lawyer_row::AssignDeveloperLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::BuyPropertyShares(instruction) => {
                let row = buy_property_shares_row::BuyPropertySharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::BuyRelistedShares(instruction) => {
                let row = buy_relisted_shares_row::BuyRelistedSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CancelOffer(instruction) => {
                let row = cancel_offer_row::CancelOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ClaimShares(instruction) => {
                let row = claim_shares_row::ClaimSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ClaimSpvCase(instruction) => {
                let row = claim_spv_case_row::ClaimSpvCaseRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseCancelledPosition(instruction) => {
                let row = close_cancelled_position_row::CloseCancelledPositionRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseCandidacy(instruction) => {
                let row = close_candidacy_row::CloseCandidacyRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseCase(instruction) => {
                let row = close_case_row::CloseCaseRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseDeadListing(instruction) => {
                let row = close_dead_listing_row::CloseDeadListingRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseReservation(instruction) => {
                let row = close_reservation_row::CloseReservationRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CloseShareHolding(instruction) => {
                let row = close_share_holding_row::CloseShareHoldingRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CreateSpv(instruction) => {
                let row = create_spv_row::CreateSpvRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::DelistShares(instruction) => {
                let row = delist_shares_row::DelistSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ExecuteDeal(instruction) => {
                let row = execute_deal_row::ExecuteDealRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::FinalizeSpvElection(instruction) => {
                let row = finalize_spv_election_row::FinalizeSpvElectionRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::InitPropertyAssets(instruction) => {
                let row = init_property_assets_row::InitPropertyAssetsRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::InitializeConfig(instruction) => {
                let row = initialize_config_row::InitializeConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::LawyerConfirmDocuments(instruction) => {
                let row = lawyer_confirm_documents_row::LawyerConfirmDocumentsRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ListProperty(instruction) => {
                let row = list_property_row::ListPropertyRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::LockShares(instruction) => {
                let row = lock_shares_row::LockSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::MakeOffer(instruction) => {
                let row = make_offer_row::MakeOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::RegisterLawyer(instruction) => {
                let row = register_lawyer_row::RegisterLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::RejectOffer(instruction) => {
                let row = reject_offer_row::RejectOfferRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ReleaseReservation(instruction) => {
                let row = release_reservation_row::ReleaseReservationRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::RelistShares(instruction) => {
                let row = relist_shares_row::RelistSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ReserveShares(instruction) => {
                let row = reserve_shares_row::ReserveSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ResignFromCase(instruction) => {
                let row = resign_from_case_row::ResignFromCaseRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::ResolveSilentVerdict(instruction) => {
                let row = resolve_silent_verdict_row::ResolveSilentVerdictRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::SendPropertyShares(instruction) => {
                let row = send_property_shares_row::SendPropertySharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::SettleCancelledFees(instruction) => {
                let row = settle_cancelled_fees_row::SettleCancelledFeesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnlockShares(instruction) => {
                let row = unlock_shares_row::UnlockSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnlockVotingShares(instruction) => {
                let row = unlock_voting_shares_row::UnlockVotingSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnregisterLawyer(instruction) => {
                let row = unregister_lawyer_row::UnregisterLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UnreserveShares(instruction) => {
                let row = unreserve_shares_row::UnreserveSharesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UpdateAuthority(instruction) => {
                let row = update_authority_row::UpdateAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UpdateConfig(instruction) => {
                let row = update_config_row::UpdateConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::UpgradeObject(instruction) => {
                let row = upgrade_object_row::UpgradeObjectRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::VoteOnSpvLawyer(instruction) => {
                let row = vote_on_spv_lawyer_row::VoteOnSpvLawyerRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawCancelled(instruction) => {
                let row = withdraw_cancelled_row::WithdrawCancelledRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawDepositUnsold(instruction) => {
                let row = withdraw_deposit_unsold_row::WithdrawDepositUnsoldRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawExpired(instruction) => {
                let row = withdraw_expired_row::WithdrawExpiredRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::WithdrawLegalProcessExpired(instruction) => {
                let row = withdraw_legal_process_expired_row::WithdrawLegalProcessExpiredRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MarketplaceInstruction::CpiEvent(instruction) => {
                let row = cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
