//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLEnum;

/// Why shares are locked. Each reason keeps its own counter on the holding
/// and the effective lock is the largest of them, so backing one vote never
/// spends weight another kind of vote could still use.
#[derive(Debug, Clone, GraphQLEnum)]
#[graphql(name = "LockReason")]
pub enum LockReasonGraphQL {
        LawyerElection,
        AgentElection,
        Proposal,
        Challenge,
}

impl From<crate::types::LockReason> for LockReasonGraphQL {
    fn from(original: crate::types::LockReason) -> Self {
        match original {
            crate::types::LockReason::LawyerElection => Self::LawyerElection,
            crate::types::LockReason::AgentElection => Self::AgentElection,
            crate::types::LockReason::Proposal => Self::Proposal,
            crate::types::LockReason::Challenge => Self::Challenge,
        }
    }
}

