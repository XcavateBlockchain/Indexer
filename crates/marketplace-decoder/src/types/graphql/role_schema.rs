//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLEnum;

/// All roles recognised across the realXmarket protocol.
/// 
/// A role is app-level authorization, separate from KYC/compliance. Compliance
/// lives in [`AccessPermission`], and will eventually be driven by SAS
/// attestations rather than a manually set flag.
/// 
/// Do not reorder the variants: the serialized `RoleAccount.role` stores the
/// variant index, so a reorder reinterprets every existing assignment.
#[derive(Debug, Clone, GraphQLEnum)]
#[graphql(name = "Role")]
pub enum RoleGraphQL {
        RegionalOperator,
        RealEstateInvestor,
        RealEstateDeveloper,
        Lawyer,
        LettingAgent,
        SpvConfirmation,
}

impl From<crate::types::Role> for RoleGraphQL {
    fn from(original: crate::types::Role) -> Self {
        match original {
            crate::types::Role::RegionalOperator => Self::RegionalOperator,
            crate::types::Role::RealEstateInvestor => Self::RealEstateInvestor,
            crate::types::Role::RealEstateDeveloper => Self::RealEstateDeveloper,
            crate::types::Role::Lawyer => Self::Lawyer,
            crate::types::Role::LettingAgent => Self::LettingAgent,
            crate::types::Role::SpvConfirmation => Self::SpvConfirmation,
        }
    }
}

