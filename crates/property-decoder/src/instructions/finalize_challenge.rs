//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct FinalizeChallenge {
    pub asset_id: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct FinalizeChallengeInstructionAccounts {
    pub cranker: solana_pubkey::Pubkey,
    pub rent_payer: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub letting: solana_pubkey::Pubkey,
    pub property: solana_pubkey::Pubkey,
    pub challenge: solana_pubkey::Pubkey,
    pub agent_entry: Option<solana_pubkey::Pubkey>,
    pub xcav_mint: solana_pubkey::Pubkey,
    pub vault: solana_pubkey::Pubkey,
    pub treasury: solana_pubkey::Pubkey,
    pub treasury_token: solana_pubkey::Pubkey,
    pub challenger: solana_pubkey::Pubkey,
    pub challenger_token: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl FinalizeChallenge {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[184, 38, 132, 51, 103, 143, 203, 9] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for FinalizeChallenge {
    type ArrangedAccounts = FinalizeChallengeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let cranker = next_account(&mut iter)?;
        let rent_payer = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let letting = next_account(&mut iter)?;
        let property = next_account(&mut iter)?;
        let challenge = next_account(&mut iter)?;
        let agent_entry = next_account(&mut iter);
        let xcav_mint = next_account(&mut iter)?;
        let vault = next_account(&mut iter)?;
        let treasury = next_account(&mut iter)?;
        let treasury_token = next_account(&mut iter)?;
        let challenger = next_account(&mut iter)?;
        let challenger_token = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(FinalizeChallengeInstructionAccounts {
            cranker,
            rent_payer,
            config,
            letting,
            property,
            challenge,
            agent_entry,
            xcav_mint,
            vault,
            treasury,
            treasury_token,
            challenger,
            challenger_token,
            token_program,
            associated_token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
