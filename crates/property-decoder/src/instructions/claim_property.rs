//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct ClaimProperty {
    pub asset_id: u64,
    pub round: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ClaimPropertyInstructionAccounts {
    pub agent: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub config: solana_pubkey::Pubkey,
    pub agent_role: solana_pubkey::Pubkey,
    pub agent_entry: solana_pubkey::Pubkey,
    pub property: solana_pubkey::Pubkey,
    pub letting: solana_pubkey::Pubkey,
    pub candidacy: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ClaimProperty {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[32, 173, 214, 197, 6, 43, 249, 5] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ClaimProperty {
    type ArrangedAccounts = ClaimPropertyInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let agent = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let config = next_account(&mut iter)?;
        let agent_role = next_account(&mut iter)?;
        let agent_entry = next_account(&mut iter)?;
        let property = next_account(&mut iter)?;
        let letting = next_account(&mut iter)?;
        let candidacy = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ClaimPropertyInstructionAccounts {
            agent,
            payer,
            config,
            agent_role,
            agent_entry,
            property,
            letting,
            candidacy,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
