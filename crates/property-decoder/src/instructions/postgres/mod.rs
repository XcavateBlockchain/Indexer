//! This code was AUTOGENERATED using the Codama library.
pub mod accept_authority_row;
pub mod add_letting_agent_row;
pub mod challenge_agent_row;
pub mod claim_income_row;
pub mod claim_property_row;
pub mod close_agent_candidacy_row;
pub mod close_income_checkpoint_row;
pub mod distribute_income_row;
pub mod finalize_agent_election_row;
pub mod finalize_challenge_row;
pub mod finalize_proposal_row;
pub mod finalize_resignation_row;
pub mod initialize_config_row;
pub mod propose_row;
pub mod remove_letting_agent_row;
pub mod resign_row;
pub mod settle_income_row;
pub mod unlock_agent_votes_row;
pub mod unlock_challenge_votes_row;
pub mod unlock_proposal_votes_row;
pub mod update_authority_row;
pub mod update_config_row;
pub mod vote_on_agent_row;
pub mod vote_on_challenge_row;
pub mod vote_on_proposal_row;
pub mod cpi_event_row;

pub use self::accept_authority_row::*;
pub use self::add_letting_agent_row::*;
pub use self::challenge_agent_row::*;
pub use self::claim_income_row::*;
pub use self::claim_property_row::*;
pub use self::close_agent_candidacy_row::*;
pub use self::close_income_checkpoint_row::*;
pub use self::distribute_income_row::*;
pub use self::finalize_agent_election_row::*;
pub use self::finalize_challenge_row::*;
pub use self::finalize_proposal_row::*;
pub use self::finalize_resignation_row::*;
pub use self::initialize_config_row::*;
pub use self::propose_row::*;
pub use self::remove_letting_agent_row::*;
pub use self::resign_row::*;
pub use self::settle_income_row::*;
pub use self::unlock_agent_votes_row::*;
pub use self::unlock_challenge_votes_row::*;
pub use self::unlock_proposal_votes_row::*;
pub use self::update_authority_row::*;
pub use self::update_config_row::*;
pub use self::vote_on_agent_row::*;
pub use self::vote_on_challenge_row::*;
pub use self::vote_on_proposal_row::*;
pub use self::cpi_event_row::*;

use super::PropertyInstruction;

pub struct PropertyInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for PropertyInstructionsMigration {
    fn app(&self) -> &str {
        "property"
    }

    fn name(&self) -> &str {
        "property_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AcceptAuthorityMigrationOperation),
            Box::new(AddLettingAgentMigrationOperation),
            Box::new(ChallengeAgentMigrationOperation),
            Box::new(ClaimIncomeMigrationOperation),
            Box::new(ClaimPropertyMigrationOperation),
            Box::new(CloseAgentCandidacyMigrationOperation),
            Box::new(CloseIncomeCheckpointMigrationOperation),
            Box::new(DistributeIncomeMigrationOperation),
            Box::new(FinalizeAgentElectionMigrationOperation),
            Box::new(FinalizeChallengeMigrationOperation),
            Box::new(FinalizeProposalMigrationOperation),
            Box::new(FinalizeResignationMigrationOperation),
            Box::new(InitializeConfigMigrationOperation),
            Box::new(ProposeMigrationOperation),
            Box::new(RemoveLettingAgentMigrationOperation),
            Box::new(ResignMigrationOperation),
            Box::new(SettleIncomeMigrationOperation),
            Box::new(UnlockAgentVotesMigrationOperation),
            Box::new(UnlockChallengeVotesMigrationOperation),
            Box::new(UnlockProposalVotesMigrationOperation),
            Box::new(UpdateAuthorityMigrationOperation),
            Box::new(UpdateConfigMigrationOperation),
            Box::new(VoteOnAgentMigrationOperation),
            Box::new(VoteOnChallengeMigrationOperation),
            Box::new(VoteOnProposalMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct PropertyInstructionWithMetadata(pub PropertyInstruction, pub carbon_core::instruction::InstructionMetadata);

impl From<(PropertyInstruction, carbon_core::instruction::InstructionMetadata)> for PropertyInstructionWithMetadata {
    fn from(value: (PropertyInstruction, carbon_core::instruction::InstructionMetadata)) -> Self {
        PropertyInstructionWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for PropertyInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let PropertyInstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            PropertyInstruction::AcceptAuthority(instruction) => {
                let row = accept_authority_row::AcceptAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::AddLettingAgent(instruction) => {
                let row = add_letting_agent_row::AddLettingAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::ChallengeAgent(instruction) => {
                let row = challenge_agent_row::ChallengeAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::ClaimIncome(instruction) => {
                let row = claim_income_row::ClaimIncomeRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::ClaimProperty(instruction) => {
                let row = claim_property_row::ClaimPropertyRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::CloseAgentCandidacy(instruction) => {
                let row = close_agent_candidacy_row::CloseAgentCandidacyRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::CloseIncomeCheckpoint(instruction) => {
                let row = close_income_checkpoint_row::CloseIncomeCheckpointRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::DistributeIncome(instruction) => {
                let row = distribute_income_row::DistributeIncomeRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeAgentElection(instruction) => {
                let row = finalize_agent_election_row::FinalizeAgentElectionRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeChallenge(instruction) => {
                let row = finalize_challenge_row::FinalizeChallengeRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeProposal(instruction) => {
                let row = finalize_proposal_row::FinalizeProposalRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeResignation(instruction) => {
                let row = finalize_resignation_row::FinalizeResignationRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::InitializeConfig(instruction) => {
                let row = initialize_config_row::InitializeConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::Propose(instruction) => {
                let row = propose_row::ProposeRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::RemoveLettingAgent(instruction) => {
                let row = remove_letting_agent_row::RemoveLettingAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::Resign(instruction) => {
                let row = resign_row::ResignRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::SettleIncome(instruction) => {
                let row = settle_income_row::SettleIncomeRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UnlockAgentVotes(instruction) => {
                let row = unlock_agent_votes_row::UnlockAgentVotesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UnlockChallengeVotes(instruction) => {
                let row = unlock_challenge_votes_row::UnlockChallengeVotesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UnlockProposalVotes(instruction) => {
                let row = unlock_proposal_votes_row::UnlockProposalVotesRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UpdateAuthority(instruction) => {
                let row = update_authority_row::UpdateAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UpdateConfig(instruction) => {
                let row = update_config_row::UpdateConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::VoteOnAgent(instruction) => {
                let row = vote_on_agent_row::VoteOnAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::VoteOnChallenge(instruction) => {
                let row = vote_on_challenge_row::VoteOnChallengeRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::VoteOnProposal(instruction) => {
                let row = vote_on_proposal_row::VoteOnProposalRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            PropertyInstruction::CpiEvent(instruction) => {
                let row = cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for PropertyInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let PropertyInstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            PropertyInstruction::AcceptAuthority(instruction) => {
                let row = accept_authority_row::AcceptAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::AddLettingAgent(instruction) => {
                let row = add_letting_agent_row::AddLettingAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::ChallengeAgent(instruction) => {
                let row = challenge_agent_row::ChallengeAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::ClaimIncome(instruction) => {
                let row = claim_income_row::ClaimIncomeRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::ClaimProperty(instruction) => {
                let row = claim_property_row::ClaimPropertyRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::CloseAgentCandidacy(instruction) => {
                let row = close_agent_candidacy_row::CloseAgentCandidacyRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::CloseIncomeCheckpoint(instruction) => {
                let row = close_income_checkpoint_row::CloseIncomeCheckpointRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::DistributeIncome(instruction) => {
                let row = distribute_income_row::DistributeIncomeRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeAgentElection(instruction) => {
                let row = finalize_agent_election_row::FinalizeAgentElectionRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeChallenge(instruction) => {
                let row = finalize_challenge_row::FinalizeChallengeRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeProposal(instruction) => {
                let row = finalize_proposal_row::FinalizeProposalRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::FinalizeResignation(instruction) => {
                let row = finalize_resignation_row::FinalizeResignationRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::InitializeConfig(instruction) => {
                let row = initialize_config_row::InitializeConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::Propose(instruction) => {
                let row = propose_row::ProposeRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::RemoveLettingAgent(instruction) => {
                let row = remove_letting_agent_row::RemoveLettingAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::Resign(instruction) => {
                let row = resign_row::ResignRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::SettleIncome(instruction) => {
                let row = settle_income_row::SettleIncomeRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UnlockAgentVotes(instruction) => {
                let row = unlock_agent_votes_row::UnlockAgentVotesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UnlockChallengeVotes(instruction) => {
                let row = unlock_challenge_votes_row::UnlockChallengeVotesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UnlockProposalVotes(instruction) => {
                let row = unlock_proposal_votes_row::UnlockProposalVotesRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UpdateAuthority(instruction) => {
                let row = update_authority_row::UpdateAuthorityRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::UpdateConfig(instruction) => {
                let row = update_config_row::UpdateConfigRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::VoteOnAgent(instruction) => {
                let row = vote_on_agent_row::VoteOnAgentRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::VoteOnChallenge(instruction) => {
                let row = vote_on_challenge_row::VoteOnChallengeRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::VoteOnProposal(instruction) => {
                let row = vote_on_proposal_row::VoteOnProposalRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            PropertyInstruction::CpiEvent(instruction) => {
                let row = cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
