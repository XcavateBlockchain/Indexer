//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct UnlockChallengeVotes {
    pub asset_id: u64,
    pub id: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UnlockChallengeVotesInstructionAccounts {
    pub voter: solana_pubkey::Pubkey,
    pub rent_payer: solana_pubkey::Pubkey,
    pub challenge: solana_pubkey::Pubkey,
    pub holding: solana_pubkey::Pubkey,
    pub vote_record: solana_pubkey::Pubkey,
    pub cpi_auth: solana_pubkey::Pubkey,
    pub marketplace_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UnlockChallengeVotes {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[141, 196, 219, 149, 47, 78, 103, 248] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for UnlockChallengeVotes {
    type ArrangedAccounts = UnlockChallengeVotesInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let voter = next_account(&mut iter)?;
        let rent_payer = next_account(&mut iter)?;
        let challenge = next_account(&mut iter)?;
        let holding = next_account(&mut iter)?;
        let vote_record = next_account(&mut iter)?;
        let cpi_auth = next_account(&mut iter)?;
        let marketplace_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UnlockChallengeVotesInstructionAccounts {
            voter,
            rent_payer,
            challenge,
            holding,
            vote_record,
            cpi_auth,
            marketplace_program,
            remaining: remaining.to_vec(),
        })
    }
}
