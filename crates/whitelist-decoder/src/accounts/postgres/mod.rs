//! This code was AUTOGENERATED using the Codama library.
pub mod admin_row;
pub mod config_row;
pub mod role_account_row;

pub use self::admin_row::*;
pub use self::config_row::*;
pub use self::role_account_row::*;

use super::XcavateWhitelistAccount;

pub struct XcavateWhitelistAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for XcavateWhitelistAccountsMigration {
    fn app(&self) -> &str {
        "xcavate-whitelist"
    }

    fn name(&self) -> &str {
        "xcavate_whitelist_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AdminMigrationOperation),
            Box::new(ConfigMigrationOperation),
            Box::new(RoleAccountMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct XcavateWhitelistAccountWithMetadata(pub XcavateWhitelistAccount, pub carbon_core::account::AccountMetadata);

impl From<(XcavateWhitelistAccount, carbon_core::account::AccountMetadata)> for XcavateWhitelistAccountWithMetadata {
    fn from(value: (XcavateWhitelistAccount, carbon_core::account::AccountMetadata)) -> Self {
        XcavateWhitelistAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for XcavateWhitelistAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let XcavateWhitelistAccountWithMetadata(account, metadata) = self;

        match account {
            XcavateWhitelistAccount::Admin(account) => {
                let row = admin_row::AdminRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            XcavateWhitelistAccount::Config(account) => {
                let row = config_row::ConfigRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            XcavateWhitelistAccount::RoleAccount(account) => {
                let row = role_account_row::RoleAccountRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for XcavateWhitelistAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let XcavateWhitelistAccountWithMetadata(account, metadata) = self;
        match account {
            XcavateWhitelistAccount::Admin(account) => {
                let row = admin_row::AdminRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            XcavateWhitelistAccount::Config(account) => {
                let row = config_row::ConfigRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            XcavateWhitelistAccount::RoleAccount(account) => {
                let row = role_account_row::RoleAccountRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

