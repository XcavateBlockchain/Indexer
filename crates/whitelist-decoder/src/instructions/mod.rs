//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::XcavateWhitelistDecoder;
#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod accept_authority;
pub mod add_admin;
pub mod assign_role;
pub mod initialize_config;
pub mod remove_admin;
pub mod remove_role;
pub mod renounce_role;
pub mod set_permission;
pub mod update_authority;
pub mod cpi_event;

pub use self::accept_authority::*;
pub use self::add_admin::*;
pub use self::assign_role::*;
pub use self::initialize_config::*;
pub use self::remove_admin::*;
pub use self::remove_role::*;
pub use self::renounce_role::*;
pub use self::set_permission::*;
pub use self::update_authority::*;
pub use self::cpi_event::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum XcavateWhitelistInstruction {
    AcceptAuthority(AcceptAuthority),
    AddAdmin(AddAdmin),
    AssignRole(AssignRole),
    InitializeConfig(InitializeConfig),
    RemoveAdmin(RemoveAdmin),
    RemoveRole(RemoveRole),
    RenounceRole(RenounceRole),
    SetPermission(SetPermission),
    UpdateAuthority(UpdateAuthority),
    // Anchor CPI Event Instruction
    CpiEvent(CpiEvent)
}

impl carbon_core::instruction::InstructionDecoder<'_> for XcavateWhitelistDecoder {
    type InstructionType = XcavateWhitelistInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<carbon_core::instruction::DecodedInstruction<Self::InstructionType>> {
        if !instruction.program_id.eq(&PROGRAM_ID) {
            return None;
        }

        let data = instruction.data.as_slice();

        {
            if let Some(decoded) = accept_authority::AcceptAuthority::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::AcceptAuthority(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = add_admin::AddAdmin::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::AddAdmin(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = assign_role::AssignRole::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::AssignRole(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = initialize_config::InitializeConfig::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::InitializeConfig(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_admin::RemoveAdmin::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::RemoveAdmin(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = remove_role::RemoveRole::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::RemoveRole(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = renounce_role::RenounceRole::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::RenounceRole(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = set_permission::SetPermission::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::SetPermission(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = update_authority::UpdateAuthority::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::UpdateAuthority(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }
        {
            if let Some(decoded) = cpi_event::CpiEvent::decode(data) {
                return Some(carbon_core::instruction::DecodedInstruction {
                    program_id: instruction.program_id,
                    data: XcavateWhitelistInstruction::CpiEvent(decoded),
                    accounts: instruction.accounts.clone(),
                });
            }
        }

        None
    }
}
