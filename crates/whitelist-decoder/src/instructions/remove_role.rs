//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use crate::types::Role;

/// Remove a role from a user. Admin-only.

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct RemoveRole {
    pub role: Role,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RemoveRoleInstructionAccounts {
    pub admin_signer: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub user: solana_pubkey::Pubkey,
    pub rent_payer: solana_pubkey::Pubkey,
    pub role_account: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl RemoveRole {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[74, 69, 168, 163, 248, 3, 130, 0] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for RemoveRole {
    type ArrangedAccounts = RemoveRoleInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let admin_signer = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let user = next_account(&mut iter)?;
        let rent_payer = next_account(&mut iter)?;
        let role_account = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(RemoveRoleInstructionAccounts {
            admin_signer,
            admin,
            user,
            rent_payer,
            role_account,
            remaining: remaining.to_vec(),
        })
    }
}
